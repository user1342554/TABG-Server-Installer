# New portable preset

Replace this file with the preset's behavior, requirements, controls and known
incompatibilities. Copy mode-specific server files into `server/` and paired
client files into `client/`. Never include passwords, player permissions, logs,
join codes or player IDs.
