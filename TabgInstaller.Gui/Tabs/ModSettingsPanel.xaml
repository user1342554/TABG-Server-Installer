<UserControl x:Class="TabgInstaller.Gui.Tabs.ModSettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="5">
        <StackPanel Margin="8">

            <!-- StarterPack Fixes -->
            <GroupBox Header="StarterPack Fixes" Margin="0,0,0,8">
                <StackPanel Margin="8">
                    <CheckBox x:Name="ChkEnableLootDrops" Content="Enable Loot Drops"
                              ToolTip="When enabled, items drop from loot containers. Disable for loadout-only modes."/>
                </StackPanel>
            </GroupBox>

            <!-- Grenades On Death - Attacker -->
            <GroupBox Header="Grenades On Death - Attacker" Margin="0,0,0,8">
                <StackPanel Margin="8">
                    <CheckBox x:Name="ChkAttackerEnabled" Content="Enabled" Margin="0,0,0,6"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <Label Content="Chance (0.0 - 1.0):" Width="130"/>
                        <TextBox x:Name="TxtAttackerChance" Width="80"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Label Content="Grenade:" Width="130"/>
                        <ComboBox x:Name="CmbAttackerGrenade" Width="220"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <!-- Grenades On Death - Corpse -->
            <GroupBox Header="Grenades On Death - Corpse" Margin="0,0,0,8">
                <StackPanel Margin="8">
                    <CheckBox x:Name="ChkCorpseEnabled" Content="Enabled" Margin="0,0,0,6"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <Label Content="Chance (0.0 - 1.0):" Width="130"/>
                        <TextBox x:Name="TxtCorpseChance" Width="80"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Label Content="Grenade:" Width="130"/>
                        <ComboBox x:Name="CmbCorpseGrenade" Width="220"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <!-- Lives & Advanced -->
            <GroupBox Header="Lives &amp; Advanced" Margin="0,0,0,8">
                <StackPanel Margin="8">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <Label Content="Lives:" Width="130"/>
                        <TextBox x:Name="TxtLives" Width="80"/>
                        <TextBlock Text="(256 = infinite)" VerticalAlignment="Center" Margin="8,0,0,0" Foreground="Gray"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Label Content="Streaming Distance:" Width="130"/>
                        <TextBox x:Name="TxtStreamingDistance" Width="80"/>
                        <TextBlock Text="(-1 = normal)" VerticalAlignment="Center" Margin="8,0,0,0" Foreground="Gray"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <!-- Ban List -->
            <GroupBox Header="Ban List" Margin="0,0,0,8">
                <StackPanel Margin="8">
                    <TextBlock Text="One Epic ID per line" Foreground="Gray" Margin="0,0,0,4"/>
                    <TextBox x:Name="TxtBanList" Height="120" AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"
                             FontFamily="Consolas" FontSize="12"/>
                </StackPanel>
            </GroupBox>

            <!-- Proximity Chat -->
            <GroupBox Header="Proximity Chat" Margin="0,0,0,8">
                <StackPanel Margin="8">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <Label Content="Max Range (m):" Width="130"/>
                        <TextBox x:Name="TxtProxChatMaxRange" Width="80"/>
                        <TextBlock Text="(default 50)" VerticalAlignment="Center" Margin="8,0,0,0" Foreground="Gray"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <Label Content="Min Range (m):" Width="130"/>
                        <TextBox x:Name="TxtProxChatMinRange" Width="80"/>
                        <TextBlock Text="(default 5)" VerticalAlignment="Center" Margin="8,0,0,0" Foreground="Gray"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <Label Content="Falloff Curve:" Width="130"/>
                        <ComboBox x:Name="CmbProxChatFalloff" Width="140">
                            <ComboBoxItem Content="Linear"/>
                            <ComboBoxItem Content="Logarithmic"/>
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Label Content="Voice Port:" Width="130"/>
                        <TextBox x:Name="TxtProxChatVoicePort" Width="80"/>
                        <TextBlock Text="(default 7778)" VerticalAlignment="Center" Margin="8,0,0,0" Foreground="Gray"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <!-- Bottom bar -->
            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                <Button Content="Save" Width="100" Click="SaveButton_Click"/>
                <TextBlock x:Name="StatusText" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>
